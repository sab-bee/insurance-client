import React, { useEffect, useState } from "react";

const useCheckPayment = ({ pacakgeId, user }) => {
  const [payCheck, setPayCheck] = useState({});
  useEffect(() => {
    
  }, [pacakgeId]);
};

export default useCheckPayment;
